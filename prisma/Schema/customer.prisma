// model Customer {
//   id        Int      @id @default(autoincrement())
//   name      String
//   email     String   @unique
//   age       Int?
//   address   String?
//   contact   String?

//   isDeleted Boolean  @default(false)
//   deletedAt DateTime?
//   updatedAt DateTime @updatedAt
//   createdAt DateTime @default(now())

//   // relations
//   userID  String @unique
//   user    User   @relation(fields: [userID], references: [id], onDelete: Cascade, onUpdate: Cascade)

//   // customerOrders CustomerOrder[]  // Uncomment when CustomerOrder model is created

//   @@index([email], name: "idx_customer_email")
//   @@index([isDeleted], name: "idx_customer_isDeleted")
//   @@map("customer")
// }

model Customer {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  age       Int?
  address   String?
  contact   String?

  isDeleted Boolean  @default(false)
  deletedAt DateTime?
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())

  // relations
  userID  String @unique
  user    User   @relation(fields: [userID], references: [id], onDelete: Cascade, onUpdate: Cascade)

  // customerOrders CustomerOrder[]  // Uncomment when CustomerOrder model is created

  @@index([email], name: "idx_customer_email")
  @@index([isDeleted], name: "idx_customer_isDeleted")
  @@map("customer")
}