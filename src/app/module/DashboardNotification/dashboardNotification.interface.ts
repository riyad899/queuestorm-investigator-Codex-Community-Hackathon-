export interface IDashboardNotificationSettingPayload {
  orderUpdates?: boolean;
  lowStockAlerts?: boolean;
  newUserRegistrations?: boolean;
  revenueReports?: boolean;
  promotionsOffers?: boolean;
}
